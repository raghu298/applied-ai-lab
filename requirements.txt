# Streamlit Community Cloud dependencies for the health_assist lite demo.
# The full local environment is cloud-native-web/health_assist/requirements.txt.
#
# torch is pinned to the +cpu build. The default PyPI wheel bundles the CUDA
# runtime, which unpacks to roughly 2.5 GB and on its own exceeds the memory
# the community tier allows, on a machine that has no GPU to use it with.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.60.0
torch==2.13.0+cpu
transformers==5.14.1
sentence-transformers==5.6.1
pandas==2.3.3
numpy==2.4.6
plotly==6.9.0
librosa==0.11.0
soundfile==0.14.0
